// process.env
export default {
    PORT:process.env.PORT,
    jwt_secret:process.env.JWT_SECRET,
    node_mode:process.env.NODE_MODE,
    url_deployed:process.env.URL_DEPLOYED,
    stripe_sk:process.env.STRIPE_SK,
    stripe_pk:process.env.STRIPE_PK,
}